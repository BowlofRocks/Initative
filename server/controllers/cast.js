module.exports = {
    getCampaigns: (req, res) => {
        // Send user_id and find all campaign_ids associated with it
    },

    getCharacters: (req, res) => {
        //Send user_id and campaign_id to find characters  
    },

    getMonsters: (req, res) => {
        //Send user_id and campaign_id to find monsters
    }
}